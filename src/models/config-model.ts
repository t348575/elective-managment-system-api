export type ConfigModel = {
	port: number;
	instances: number;
	privateKey: string;
	publicKey: string;
	serverAddress: string;
	mailHost: string;
	mailUser: string;
	mailPassword: string;
	mailName: string;
}
