export type ConfigModel = {
	port: number;
	host: string;
	user: string;
	password: string;
	database: string;
	dbPort: number;
	instances: number;
	privateKey: string;
	publicKey: string;
	serverAddress: string;
}
